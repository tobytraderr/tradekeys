export default function ActivityPage() {
  return (
    <div className="stack">
      <h1 className="section-title">Activity</h1>
      <p className="section-subtitle">Market-wide indexed twin activity feed</p>
      <section className="card">
        Activity feed wiring will consume the subgraph trade and metadata update streams as the next implementation step.
      </section>
    </div>
  )
}
